# Basic Auth credentials for Traefik
# Generate password hash with: htpasswd -nb username password
# Or use: echo $(htpasswd -nb username password) | sed -e s/\\$/\\$\\$/g
# Note: Dollar signs must be doubled ($$) when used in docker-compose
BASIC_AUTH_USERS=testuser:$$apr1$$xxxxxxxx$$yyyyyyyyyyy
